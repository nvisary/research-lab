# btc_eth_pair

Smoke-test stat-arb strategy. Trivial BTC + β·(−ETH) pair with OLS hedge
ratio re-estimated every refit (default 7d on 1h). Exists to validate
the harness_statarb engine end-to-end — not to be a real edge.

## Iter log

(none yet — first run pending)
